---
import BaseLayout from "../layouts/BaseLayout.astro";
import {
  contact,
  education,
  papers,
  profile,
  research,
  researchInterests,
  siteMeta,
  teaching
} from "../data/site";
---

<BaseLayout title={siteMeta.title} description={siteMeta.description}>
  <main class="page-shell">
    <header class="hero" id="top">
      <div class="hero-nav">
        <span class="hero-mark">Ruihan Lin</span>
        <nav aria-label="Primary">
          <a href="#about">About</a>
          <a href="#research">Research</a>
          <a href="#education">Education</a>
          <a href="#papers">Papers</a>
          <a href="#teaching">Teaching</a>
          <a href="#contact">Contact</a>
        </nav>
      </div>

      <div class="hero-grid">
        <div class="hero-copy">
          <h1>{profile.name}</h1>
          <p class="hero-role">{profile.role}</p>
          <p class="hero-meta">{profile.institution}</p>
          <p class="hero-meta">Advised by {profile.advisor}</p>
          <p class="hero-intro">{profile.intro}</p>

          <div class="hero-links">
            {profile.links.map((link) => (
              <a href={link.href}>{link.label}</a>
            ))}
          </div>
        </div>

        <div class="hero-photo-wrap">
          <img
            class="hero-photo"
            src={profile.photo}
            alt={`Portrait of ${profile.name}`}
            width="320"
            height="400"
          />
        </div>
      </div>
    </header>

    <section class="section" id="about">
      <div class="section-heading">
        <p>About Me</p>
      </div>
      <div class="section-body prose">
        {profile.bio.map((paragraph) => <p>{paragraph}</p>)}
      </div>
    </section>

    <section class="section" id="research">
      <div class="section-heading">
        <p>Research Interest</p>
      </div>
      <div class="section-body prose">
        <ul class="interest-list">
          {researchInterests.map((item) => <li>{item}</li>)}
        </ul>
        {research.summary.map((paragraph) => <p>{paragraph}</p>)}
      </div>
    </section>

    <section class="section" id="education">
      <div class="section-heading">
        <p>Education</p>
      </div>
      <div class="section-body entry-list">
        {education.map((entry) => (
          <article class="entry-card">
            <div class="entry-header">
              <h2>{entry.school}</h2>
              <p class="entry-period">{entry.period}</p>
            </div>
            <p class="entry-title">{entry.degree}</p>
            {entry.details.length > 0 && (
              <ul class="entry-details">
                {entry.details.map((detail) => <li>{detail}</li>)}
              </ul>
            )}
          </article>
        ))}
      </div>
    </section>

    <section class="section" id="papers">
      <div class="section-heading">
        <p>Working Papers</p>
      </div>
      <div class="section-body paper-list-simple">
        {papers.map((paper) => (
          <article class="paper-item">
            <h2>
              <a href={paper.href} target="_blank" rel="noreferrer">
                {paper.title}
              </a>
            </h2>
            <p class="paper-authors-simple">{paper.authors}</p>
            <p class="paper-venue">{paper.venue}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="section" id="teaching">
      <div class="section-heading">
        <p>Teaching</p>
      </div>
      <div class="section-body entry-list">
        {teaching.map((entry) => (
          <article class="entry-card">
            <div class="entry-header">
              <h2>{entry.title}</h2>
              <p class="entry-period">{entry.period}</p>
            </div>
            <p class="entry-title">{entry.organization}</p>
            {entry.details.length > 0 && (
              <ul class="entry-details">
                {entry.details.map((detail) => <li>{detail}</li>)}
              </ul>
            )}
          </article>
        ))}
      </div>
    </section>

    <section class="section" id="contact">
      <div class="section-heading">
        <p>Contact</p>
      </div>
      <div class="section-body prose">
        <p>{contact.heading}</p>
        {contact.lines.map((line) => <p>{line}</p>)}
        <div class="contact-links">
          {contact.links.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </div>
      </div>
    </section>
  </main>
</BaseLayout>
